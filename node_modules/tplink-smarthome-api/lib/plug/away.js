"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const schedule_1 = require("../shared/schedule");
const utils_1 = require("../utils");
class Away {
    device;
    apiModuleName;
    childId;
    constructor(device, apiModuleName, childId = undefined) {
        this.device = device;
        this.apiModuleName = apiModuleName;
        this.childId = childId;
    }
    isSmartPath(sendOptions) {
        return this.device.shouldUseSmartMethods(sendOptions);
    }
    async ensureSmartSupported(sendOptions) {
        await this.device.negotiateSmartComponents(sendOptions);
        if (!this.device.hasComponent('antitheft', this.childId)) {
            throw new Error('Away module is not supported for this SMART device scope');
        }
    }
    toLegacyStyleResponse(response) {
        if (!(0, utils_1.isObjectLike)(response) || !Array.isArray(response.rule_list)) {
            throw new Error(`Unexpected SMART away response: ${JSON.stringify(response)}`);
        }
        const rules = response.rule_list.map((rule, index) => {
            if ((0, utils_1.isObjectLike)(rule) && typeof rule.id === 'string') {
                return rule;
            }
            if ((0, utils_1.isObjectLike)(rule)) {
                return { ...rule, id: `smart-away-${index}` };
            }
            return { id: `smart-away-${index}` };
        });
        return {
            err_code: 0,
            ...response,
            rule_list: rules,
        };
    }
    toLegacyStyleAckResponse(response) {
        if ((0, utils_1.hasErrCode)(response)) {
            return response;
        }
        if ((0, utils_1.isObjectLike)(response)) {
            return {
                err_code: 0,
                ...response,
            };
        }
        return { err_code: 0 };
    }
    /**
     * Gets Away Rules.
     *
     * Requests `anti_theft.get_rules`. Support childId.
     * @throws {@link ResponseError}
     */
    async getRules(sendOptions) {
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            return this.toLegacyStyleResponse(await this.device.sendSmartCommand('get_antitheft_rules', undefined, this.childId, sendOptions));
        }
        return (0, utils_1.extractResponse)(await this.device.sendCommand({
            [this.apiModuleName]: { get_rules: {} },
        }, this.childId, sendOptions), '', (c) => (0, schedule_1.hasRuleListWithRuleIds)(c) && (0, utils_1.hasErrCode)(c));
    }
    /**
     * Gets Away Rule.
     *
     * Requests `anti_theft.get_rules` and return rule matching Id. Support childId.
     * @throws {@link ResponseError}
     */
    async getRule(id, sendOptions) {
        const rules = await this.getRules(sendOptions);
        const rule = rules.rule_list.find((r) => r.id === id);
        if (rule === undefined)
            throw new Error(`Rule id not found: ${id}`);
        return { ...rule, err_code: rules.err_code };
    }
    /**
     * Adds Away Rule.
     *
     * Sends `anti_theft.add_rule` command and returns rule id. Support childId.
     * @returns parsed JSON response
     * @throws {@link ResponseError}
     */
    async addRule(rule, sendOptions) {
        const { start, end, daysOfWeek, frequency = 5, name = '', enable = true, } = rule;
        const awayRule = {
            frequency,
            name,
            enable: enable ? 1 : 0,
            ...(0, schedule_1.createRule)({ start, end, daysOfWeek }),
        };
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            const response = await this.device.sendSmartCommand('add_antitheft_rule', { rule: awayRule }, this.childId, sendOptions);
            return this.toLegacyStyleAckResponse(response);
        }
        return this.device.sendCommand({
            [this.apiModuleName]: { add_rule: awayRule },
        }, this.childId, sendOptions);
    }
    /**
     * Edits Away rule.
     *
     * Sends `anti_theft.edit_rule` command and returns rule id. Support childId.
     * @returns parsed JSON response
     * @throws {@link ResponseError}
     */
    async editRule(rule, sendOptions) {
        const { id, start, end, daysOfWeek, frequency = 5, name = '', enable = true, } = rule;
        const awayRule = {
            id,
            frequency,
            name,
            enable: enable ? 1 : 0,
            ...(0, schedule_1.createRule)({ start, end, daysOfWeek }),
        };
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            const response = await this.device.sendSmartCommand('edit_antitheft_rule', {
                id,
                rule: awayRule,
            }, this.childId, sendOptions);
            return this.toLegacyStyleAckResponse(response);
        }
        return this.device.sendCommand({
            [this.apiModuleName]: { edit_rule: awayRule },
        }, this.childId, sendOptions);
    }
    /**
     * Deletes All Away Rules.
     *
     * Sends `anti_theft.delete_all_rules` command. Support childId.
     * @returns parsed JSON response
     * @throws {@link ResponseError}
     */
    async deleteAllRules(sendOptions) {
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            const response = await this.device.sendSmartCommand('remove_antitheft_rules', undefined, this.childId, sendOptions);
            return this.toLegacyStyleAckResponse(response);
        }
        return this.device.sendCommand({
            [this.apiModuleName]: { delete_all_rules: {} },
        }, this.childId, sendOptions);
    }
    /**
     * Deletes Away Rule.
     *
     * Sends `anti_theft.delete_rule` command. Support childId.
     * @returns parsed JSON response
     * @throws {@link ResponseError}
     */
    async deleteRule(id, sendOptions) {
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            const response = await this.device.sendSmartCommand('remove_antitheft_rules', { ids: [id] }, this.childId, sendOptions);
            return this.toLegacyStyleAckResponse(response);
        }
        return this.device.sendCommand({
            [this.apiModuleName]: { delete_rule: { id } },
        }, this.childId, sendOptions);
    }
    /**
     * Enables or Disables Away Rules.
     *
     * Sends `anti_theft.set_overall_enable` command. Support childId.
     * @returns parsed JSON response
     * @throws {@link ResponseError}
     */
    async setOverallEnable(enable, sendOptions) {
        if (this.isSmartPath(sendOptions)) {
            await this.ensureSmartSupported(sendOptions);
            const response = await this.device.sendSmartCommand('set_antitheft_all_enable', { enable: Boolean(enable) }, this.childId, sendOptions);
            return this.toLegacyStyleAckResponse(response);
        }
        return this.device.sendCommand({
            [this.apiModuleName]: {
                set_overall_enable: { enable: enable ? 1 : 0 },
            },
        }, this.childId, sendOptions);
    }
}
exports.default = Away;
//# sourceMappingURL=away.js.map